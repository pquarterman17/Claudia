node_modules/
dist/
dist-types/
*.tsbuildinfo
.vite/
*.local
.env

# Local runtime data (settings actually live in ~/.claudia)
data/

# Claude Design artefacts: throwaway scaffolding that got this project started,
# not project source. The app's own copy of the Nocturne design tokens lives at
# web/src/nocturne.css and IS tracked. These stay local until deleted.
Conductor for parallel Claude Code projects/
design/

# Built UI (regenerated by npm start)
web/dist/
